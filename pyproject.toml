[project]
name = "jobflow"
version = "0.1.0"
description = "Job Scraping & Automation System"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "selenium",
    "webdriver-manager",
    "beautifulsoup4",
    "requests",
    "schedule",
    "streamlit",
    "python-dotenv",
]
